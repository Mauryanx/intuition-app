rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO (Milestone 2): tighten rules per data model once implemented.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
