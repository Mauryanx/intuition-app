/**
 * Firebase Cloud Functions placeholder.
 *
 * TODO (Milestone 2):
 * - implement `submitRun` callable to validate client-submitted game results.
 * - introduce scheduled leaderboard aggregation jobs.
 * - enforce auth + payload tamper checks before writing to Firestore.
 */
export const placeholder = () => {
  // Intentionally empty until backend milestone.
};
